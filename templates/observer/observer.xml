<{{eventName}}>
  <observers>
    <{{moduleName}}>
      <type>singleton</type>
      <class>{{moduleName}}/observer</class>
      <method>{{methodName}}</method>
    </{{moduleName}}>
  </observers>
</{{eventName}}>
